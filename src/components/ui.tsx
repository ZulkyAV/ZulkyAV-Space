import Link from "next/link";
import type { Folder, Note, Product, Project } from "@/types/content";

export function Eyebrow({ children }: { children: React.ReactNode }) { return <p className="mb-3 font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-primary-400">{children}</p>; }
export function SectionHeading({ title, description, href, linkLabel = "View all" }: { title: string; description?: string; href?: string; linkLabel?: string }) { return <div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 className="text-2xl font-bold tracking-tight text-neutral-50 sm:text-3xl">{title}</h2>{description && <p className="mt-2 max-w-xl text-sm leading-6 text-neutral-400">{description}</p>}</div>{href && <Link href={href} className="text-sm font-semibold text-primary-300 hover:text-primary-200">{linkLabel} <span aria-hidden="true">→</span></Link>}</div>; }
export function StatusBadge({ status }: { status: string }) { const styles: Record<string, string> = { published: "bg-success-50 text-success-700", maintenance: "bg-warning-50 text-warning-700", draft: "bg-[#1B1B23] text-neutral-400", archived: "bg-[#1B1B23] text-neutral-400", "In development": "bg-primary-50 text-primary-300", Prototype: "bg-warning-50 text-warning-700", Exploring: "bg-[#1B1B23] text-neutral-400", "In stock": "bg-success-50 text-success-700", "Low stock": "bg-warning-50 text-warning-700", "Sold out": "bg-error-50 text-error-700" }; return <span className={`inline-flex rounded-full px-2.5 py-1 text-[11px] font-semibold ${styles[status] ?? "bg-[#1B1B23] text-neutral-400"}`}>{status}</span>; }
const folderAccentClasses: Record<string, string> = { amber: "bg-accent-50", green: "bg-success-50", blue: "bg-primary-50", slate: "bg-[#1B1B23]" };

export function FolderCard({ folder, basePath }: { folder: Folder; basePath: string }) { const open = folder.status === "published"; const accentClass = folderAccentClasses[folder.accent] ?? folderAccentClasses.blue; const body = <><div className="flex items-center justify-between"><span className="text-xl">{folder.accent === "amber" ? "✦" : folder.accent === "green" ? "⌁" : "#"}</span><StatusBadge status={folder.status} /></div><h3 className="mt-6 text-lg font-bold text-neutral-100 group-hover:text-primary-300">{folder.name}</h3><p className="mt-2 min-h-12 text-sm leading-6 text-neutral-400">{folder.description}</p>{open ? <p className="mt-5 font-mono text-[11px] uppercase tracking-wider text-neutral-500">{folder.count} entries <span className="ml-1 text-primary-400">↗</span></p> : <p className="mt-5 font-mono text-[11px] uppercase tracking-wider text-warning-700">Temporarily closed</p>}</>; return <div className="group relative overflow-hidden rounded-2xl border border-white/10 bg-[#14141A] shadow-sm transition-all hover:-translate-y-1 hover:border-primary-200 hover:shadow-lg">{folder.coverImage ? <div className="relative aspect-[16/10] overflow-hidden bg-[#1B1B23]"><img src={folder.coverImage} alt={folder.coverLabel || folder.name} className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105" />{folder.coverLabel ? <span className="absolute bottom-3 left-3 max-w-[calc(100%-1.5rem)] truncate rounded-full bg-[#18181F]/95 px-3 py-1 text-[10px] font-bold text-neutral-300 shadow-sm">Featured · {folder.coverLabel}</span> : null}</div> : <div className={`h-2 ${accentClass}`} />}{open ? <Link href={`${basePath}/${folder.slug}`} className="block p-6">{body}</Link> : <div className="p-6">{body}</div>}</div>; }
export function NoteCard({ note }: { note: Note }) { return <Link href={`/note/${note.folderSlug}/${note.slug}`} className="group block border-t border-white/10 py-5"><div className="flex items-start justify-between gap-4"><div><h3 className="font-semibold text-neutral-100 transition-colors group-hover:text-primary-300">{note.title}</h3><p className="mt-2 text-sm leading-6 text-neutral-400">{note.excerpt}</p><div className="mt-3 flex gap-2">{note.tags.map((tag) => <span key={tag} className="font-mono text-[10px] uppercase tracking-wider text-primary-400">#{tag}</span>)}</div></div><span className="pt-1 text-neutral-300 transition-transform group-hover:translate-x-1 group-hover:text-primary-400">→</span></div><p className="mt-4 font-mono text-[10px] uppercase tracking-wider text-neutral-500">{note.date} · {note.readTime}</p></Link>; }
export function ProjectCard({ project }: { project: Project }) { return <Link href={`/project/${project.folderSlug}/${project.slug}`} className="group overflow-hidden rounded-2xl border border-white/10 bg-[#14141A] shadow-sm transition-all hover:-translate-y-1 hover:shadow-lg"><div className="aspect-[4/3] overflow-hidden bg-[#1B1B23]">{project.image ? <img src={project.image} alt={project.title} className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105" /> : <div className="flex h-full items-center justify-center text-sm text-neutral-500">No image yet</div>}</div><div className="p-5"><div className="mb-3 flex items-center justify-between gap-3"><StatusBadge status={project.status} /><span className="text-neutral-300 transition-transform group-hover:translate-x-1 group-hover:text-primary-400">→</span></div><h3 className="font-bold text-neutral-100 group-hover:text-primary-300">{project.title}</h3><p className="mt-2 line-clamp-3 text-sm leading-6 text-neutral-400">{project.description}</p></div></Link>; }
export function ProductCard({ product }: { product: Product }) { return <Link href={`/jualan/${product.folderSlug}/${product.slug}`} className="group overflow-hidden rounded-2xl border border-white/10 bg-[#14141A] shadow-sm transition-all hover:-translate-y-1 hover:shadow-lg"><div className="relative aspect-[4/3] overflow-hidden bg-[#1B1B23]">{product.image ? <img src={product.image} alt={product.name} className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105" /> : <div className="flex h-full items-center justify-center text-sm text-neutral-500">No image yet</div>}<div className="absolute left-3 top-3 flex flex-wrap gap-1.5">{product.labels.map((label) => <span key={label} className="rounded-full bg-[#18181F]/95 px-2.5 py-1 text-[10px] font-bold text-neutral-300 shadow-sm">{label}</span>)}</div></div><div className="p-5"><div className="mb-3 flex items-center justify-between"><StatusBadge status={product.stock} /><span className="font-mono text-xs text-neutral-300">{product.priceLabel}</span></div><h3 className="font-bold text-neutral-100 group-hover:text-primary-300">{product.name}</h3><p className="mt-2 text-sm leading-6 text-neutral-400">{product.description}</p></div></Link>; }
