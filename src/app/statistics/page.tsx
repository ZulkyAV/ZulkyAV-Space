import { Eyebrow, StatusBadge } from "@/components/ui";
import { PageShell } from "@/components/page-shell";

const periods = ["Today", "7 Days", "30 Days", "Current Month"];
const bars = [42, 58, 46, 74, 62, 88, 69, 94, 78, 66, 86, 72];

export default function StatisticsPage() { return <PageShell eyebrow="A public pulse" title="The numbers, at a distance." description="A lightweight view of what is being read, explored, and quietly enjoyed. Exact figures stay private."><div className="mb-8 flex flex-wrap gap-2">{periods.map((period, index) => <button key={period} className={`rounded-full px-4 py-2 text-sm font-semibold ${index === 1 ? "bg-neutral-900 text-white" : "border border-neutral-300 bg-white text-neutral-600 hover:border-primary-400 hover:text-primary-700"}`}>{period}</button>)}</div><div className="grid gap-5 lg:grid-cols-[1.4fr_.6fr]"><div className="rounded-2xl border border-neutral-200 bg-white p-6 shadow-sm"><div className="flex items-start justify-between"><div><p className="font-mono text-[10px] uppercase tracking-wider text-neutral-400">Views over time</p><p className="mt-2 text-3xl font-bold text-neutral-950">•••••</p><p className="mt-1 text-sm text-neutral-500">Exact numbers are private</p></div><span className="rounded-full bg-success-50 px-3 py-1 text-xs font-semibold text-success-700">+ improving</span></div><div className="mt-12 flex h-48 items-end gap-2 border-b border-neutral-200">{bars.map((height, index) => <div key={index} className="group relative flex-1"><div style={{ height: `${height}%` }} className="rounded-t-md bg-primary-200 transition-colors group-hover:bg-primary-500" /></div>)}</div><div className="mt-3 flex justify-between font-mono text-[9px] uppercase tracking-wider text-neutral-400"><span>Mon</span><span>Wed</span><span>Fri</span><span>Sun</span></div></div><div className="rounded-2xl border border-neutral-200 bg-white p-6 shadow-sm"><Eyebrow>Signal</Eyebrow><p className="mt-8 text-5xl font-bold text-neutral-950">↑</p><p className="mt-3 text-sm leading-6 text-neutral-500">More people are finding their way into the archive this month.</p><div className="mt-8 border-t border-neutral-200 pt-5"><p className="font-mono text-[10px] uppercase tracking-wider text-neutral-400">Top interest</p><p className="mt-2 font-semibold text-neutral-800">Small experiments</p></div></div></div><div className="mt-6 grid gap-5 md:grid-cols-2"><div className="rounded-2xl border border-neutral-200 bg-white p-6"><div className="flex justify-between"><h2 className="font-bold text-neutral-900">Best Seller</h2><StatusBadge status="In stock" /></div><p className="mt-5 text-lg font-semibold text-neutral-800">Field notebook set</p><p className="mt-2 font-mono text-xs text-neutral-400">Sales: ••••• · Revenue: •••••</p></div><div className="rounded-2xl border border-neutral-200 bg-white p-6"><div className="flex justify-between"><h2 className="font-bold text-neutral-900">Recommended</h2><span className="text-primary-600">✦</span></div><p className="mt-5 text-lg font-semibold text-neutral-800">Atlas of Small Wins</p><p className="mt-2 font-mono text-xs text-neutral-400">Interest: high · Status: active</p></div></div></PageShell>; }
