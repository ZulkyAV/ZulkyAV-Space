import Link from "next/link";
import { notFound } from "next/navigation";
import { Eyebrow } from "@/components/ui";
import { notes, noteFolders } from "@/data/mock-data";

export default async function NoteDetailPage({ params }: { params: Promise<{ folderSlug: string; noteSlug: string }> }) { const { folderSlug, noteSlug } = await params; const note = notes.find((entry) => entry.folderSlug === folderSlug && entry.slug === noteSlug); const folder = noteFolders.find((entry) => entry.slug === folderSlug); if (!note || !folder || folder.status !== "published") notFound(); return <article className="mx-auto max-w-3xl px-5 py-16 lg:px-8 lg:py-24"><Link href={`/note/${folder.slug}`} className="text-sm font-semibold text-primary-700">← {folder.name}</Link><header className="mt-12 border-b border-neutral-200 pb-10"><Eyebrow>{note.date} · {note.readTime}</Eyebrow><h1 className="text-4xl font-bold leading-tight tracking-tight text-neutral-950 sm:text-5xl">{note.title}</h1><div className="mt-5 flex gap-2">{note.tags.map((tag) => <span key={tag} className="font-mono text-xs text-primary-600">#{tag}</span>)}</div></header><div className="prose prose-slate mt-10 max-w-none">{note.content.map((paragraph) => <p key={paragraph} className="mb-6 text-lg leading-8 text-neutral-700">{paragraph}</p>)}</div><div className="mt-16 border-t border-neutral-200 pt-8"><Link href="/note" className="text-sm font-semibold text-primary-700">Browse all notes →</Link></div></article>; }
